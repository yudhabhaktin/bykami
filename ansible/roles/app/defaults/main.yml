---
app_name: bykami

# Path on the CONTROL machine to a linux/amd64 binary. There is deliberately no
# default: the design rule is "never build on the VPS", so this role copies a
# binary someone else built — CI, or a local cross-compile:
#
#   ./scripts/build-linux.sh          # leaves dist/bykami-linux
app_binary_src: ""

app_user: bykami
app_install_path: /usr/local/bin/bykami
app_data_dir: /var/lib/bykami

# localhost, and this is the security boundary rather than a preference. The
# tunnel dials out to reach it; binding 0.0.0.0 would expose the app on the
# public IP and make the security group load-bearing again.
app_listen_addr: 127.0.0.1:8080

# ~1.4 GiB per the memory budget. Sized against the OS and cloudflared's ~210 MB
# rather than against the box total: a limit set at capacity is not a limit,
# because the OOM killer arrives before the GC does.
app_gomemlimit: 1400MiB

# How one-time codes are delivered. Empty means the auth routes and the operator
# console's login both answer 503 — which is the correct deployed state until
# data residency is settled and a WhatsApp provider exists. "log" writes codes to
# the journal and is for development only; setting it here would put one-time
# codes in whatever reads the box's logs.
app_otp_delivery: ""

# Who may enter the operator console, comma-separated. Empty means nobody, and
# nobody is the safe default: this is a public hostname. Adding a number here is
# a configuration change with a diff and a playbook run behind it, which is the
# audit trail a role column in the database would not have given.
app_admin_phones: ""

# The shared secret booths present to pull published frames, and it must match
# booth_token in the booth role. Empty leaves /v1/booth/* answering 503, which
# is the deployed default: a box nobody configured serves no catalogue.
#
# Environment rather than a flag, for the same reason as the booth's own token —
# argv is readable by every local user through ps. Pass it from a vars file:
#   ansible-playbook site.yml --tags app -e @app.vars.yml
app_booth_token: ""

# The Instagram mirror. Empty leaves it off, and off is the deployed default:
# the sites render no feed section rather than an empty one, and /v1/social/*
# answers with an empty list.
#
# This one is a seed, not a setting, and that is the difference worth knowing
# before changing it. Meta issues a long-lived token good for 60 days and the
# app exchanges it for a fresh one before it lapses — the new token is written
# to the database, which from then on is the authority. Editing the value here
# and re-running the playbook therefore does nothing on a box that has already
# refreshed once. To genuinely replace it:
#
#   sqlite3 /var/lib/bykami/bykami.db 'DELETE FROM instagram_token'
#
# Pass it from the same vars file as the booth token, never from the inventory:
#   ansible-playbook site.yml --tags app -e @app.vars.yml
app_instagram_token: ""

# The handle being mirrored, without the @. Labelling only — the token decides
# which account is actually read, so a mismatch here mislabels a feed rather
# than fetching the wrong one.
app_instagram_account: ""

# The Google service-account key file that lets booking read and write the
# studio's calendars, base64-encoded. Empty leaves the sync off, and off is the
# deployed default: booking then runs entirely from its own database, which is
# why the busy ranges it caches are a cache and never a source.
#
# Base64 because the key is multi-line PEM and an EnvironmentFile has no syntax
# for a value containing newlines:
#
#   base64 -w0 < service-account.json
#
# A service account rather than OAuth, and the reason is operational rather than
# aesthetic: an unverified Google project issues refresh tokens that expire after
# seven days, so a consent-screen integration works for a week and then stops
# silently. A service-account key does not rotate. What it does need is for each
# calendar to be shared with the service account's own address at "Make changes
# to events" — the address is logged at startup, and the operator console's
# booking page shows which calendars have never synced.
#
# Pass it from the same vars file as the booth token, never from the inventory:
#   ansible-playbook site.yml --tags app -e @app.vars.yml
app_google_credentials: ""

# How far ahead bookings may be made. 31 days is what the YouCanBook.me pages
# this replaced offered, so it is the window the studio's customers already know.
app_booking_window_days: 31

# Fill the booking tables on a box whose catalogue is empty. On by default: a
# deployment with booking endpoints and no packages answers every availability
# request with nothing, and the failure looks like a bug rather than a missing
# step. Idempotent, and skipped entirely once anything is in the tables — so an
# owner who edits a price on the box does not have it overwritten by a redeploy.
#
# Set false if the catalogue is managed some other way.
app_booking_seed: true

app_backup_dir: /var/backups/bykami
app_backup_keep: 7
app_backup_oncalendar: "*-*-* 03:30:00"

# Off-box copies. Local backups survive rm and a bad migration; they do not
# survive losing the instance, and this instance has an auto-release scheduled
# for 2026-10-24 — a scheduled event, not a hypothetical.
#
# Off by default because it needs credentials. Enable with a vars file:
#   app_r2_enabled: true
#   app_r2_access_key_id: "..."
#   app_r2_secret_access_key: "..."
app_r2_enabled: false
app_r2_bucket: bykami-backups
app_r2_endpoint: ""
app_r2_access_key_id: ""
app_r2_secret_access_key: ""

# 30 daily copies. At the current ~72 KB per backup that is ~2 MB; the count
# matters only once the database has real customers in it.
app_r2_keep: 30

# Hard ceiling, checked against the bucket's actual contents before every
# upload. The free tier is 10 GB; this is a fifth of it, so overrunning takes a
# sustained mistake rather than one bad day.
#
# The guard exists because retention-by-count alone is not a bound: 30 copies of
# a database that has grown to 500 MB is 15 GB, and the first sign would be a
# bill. When the cap is hit the script deletes oldest-first to fit, and refuses
# rather than silently exceeding it.
app_r2_budget_bytes: 2147483648

# Pull-based deploy. CI publishes a release tagged api-<sha>; the box polls for
# it. Inverted on purpose: the VPS has one inbound rule and GitHub's runner
# ranges are too broad to allowlist without undoing the point of the tunnel.
app_release_repo: yudhabhaktin/bykami

# Every 10 minutes, offset from the hour. The cost of a poll is one API call
# against an unauthenticated rate limit of 60/hour per IP — 6/hour leaves room
# for a manual `systemctl start bykami-update` without tripping it.
app_update_oncalendar: "*:03/10"

# Off by default. A box that installs whatever main published, unattended, is a
# supply-chain path from a merged commit straight to production — so turning it
# on is a deliberate act, per host, not a side effect of running the role.
app_update_enabled: false
